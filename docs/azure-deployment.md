# Azure deployment (retired)

Production is Mini Docker. Use [deployment-mini.md](deployment-mini.md).

`pll-rg` is gone. Azure Files and ACA are not the live path. `pnpm db:upload` exits 1 and names volume `pll-lax_pll-data`.

The old Container Apps runbook, including Azure Files seeding, is [archive/azure-deployment.md](archive/azure-deployment.md). Do not follow it.

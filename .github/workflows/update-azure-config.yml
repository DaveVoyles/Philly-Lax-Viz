name: Update Azure Container App Config

# Azure Container Apps hosting is retired. CORS lives in Mini pll.env.

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Azure Container Apps path is retired
        run: |
          echo "::error::Production is Mini Docker. Edit ~/docker-stack/secrets/pll.env and recreate pll-server. Do not update ACA."
          exit 1

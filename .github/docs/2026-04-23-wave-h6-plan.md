# Wave H6 — Cross-team dedup, data-driven anomaly threshold, dev safety

## Lessons baked in
- Kill running servers before any DB mutation script (caused "database disk image is malformed" in dev today). Lane 3 codifies this.

## Lanes

| Lane | Fleet | Effort | Scope | Blocked by |
|------|-------|--------|-------|------------|
| 1 | Han 😉🚀 | M | Cross-team player dedup script + apply for known dups | — |
| 2 | Yoda 👽✨ | S | Replace `goals > 12` heuristic with data-driven 3σ threshold | — |
| 3 | Leia 👑💁‍♀️ | S | Pre-migration safety check + dev-loop docs | — |

## Lane scope contracts (locked)

### Lane 1 (Han) — Cross-team player dedup
- NEW `packages/ingest/src/scripts/dedupCrossTeam.ts` — finds players with the same exact name on multiple teams; for each pair prints a proposed merge plan with stat-row totals on each side; `--apply` to commit.
- Default keep-side rule: the player_id with the most games_played (tie-break: lower id).
- Repoint `player_stats.player_id`; delete the loser row only when no game_id collisions occur. Otherwise log warning.
- Add `dedup:cross-team` npm script.
- Apply to the 4 known dups discovered in H5 (Mason Proctor #49718/#75525, Javier Gonzalez-Cruz #49724/#78746). After --apply, run integrity check.
- Tests in `__tests__/dedupCrossTeam.test.ts` (in-memory DB).

### Lane 2 (Yoda) — Data-driven anomaly threshold
- EDIT `packages/web/src/views/playerDetail.ts` — replace hardcoded `goals > 12` heuristic.
- Compute mean+stdev of goals per game across season. Mark a row anomalous if `goals > mean + 3*stdev`.
- Stats can be derived inline from the same per-game array already on the page (`stats: PlayerPerGameStat[]`).
- Add helper `packages/web/src/util/zscore.ts` with `mean`, `stdev`, `isOutlier(value, all)`.
- Test coverage: cases for typical row, extreme row, single-row season (no anomaly possible).

### Lane 3 (Leia) — Pre-migration safety
- NEW `packages/ingest/src/scripts/lib/checkServerProcs.ts` — detects running `pll/server` processes via `pgrep -lf` and warns with PID + start time.
- WIRE into `splitCompositePlayers.ts`, `dedupCrossTeam.ts` (Han's new file), `dedupPlayers.ts`, `dedupTeams.ts`. Each `--apply` path calls the check first; if servers found and `--force` not passed, exit 1 with friendly message.
- Update `.github/copilot-instructions.md` + machine copy at `~/.github/copilot-instructions.md` — short paragraph: "Before any `--apply` migration: stop dev servers, then run."
- Tests for the helper.

## Comm log

| Time | Lane | Fleet | Update |
|------|------|-------|--------|
| 15:12 | — | Orchestrator | 📋 Plan locked, launching 3 lanes |

## Wave H6 outcomes

| Time | Lane | Fleet | Outcome |
|------|------|-------|---------|
| 15:18 | 1 | Han 😉🚀 | ✅ `dedupCrossTeam.ts` + tests landed. Discovery: **87 cross-team duplicate name groups** in live DB (incl. the 4 known: Mason Proctor #49718/#75525, Javier Gonzalez-Cruz #49724/#78746, plus typo variant Javier Gonzalez-Crus, Charlie Crossed, Chris Bryan, etc.). Wired Leia's `lib/checkServerProcs.ts` into the `--apply` path. Dry-run verified — orchestrator owns `--apply`. |

## Wave H6 outcomes

| Lane | Fleet | Outcome |
|------|-------|---------|
| 2 | Yoda 👽✨ | ✅ Replaced `goals > 12` with `isOutlier` (3σ + floor=8). New `util/zscore.ts` + 6 tests. typecheck + 34 web tests green. |
| 3 | Leia 👑💁‍♀️ | ✅ Added `checkServerProcs()` (pgrep `pll/server\|src/index\.ts`); wired into `splitCompositePlayers`/`dedupPlayers`/`dedupTeams` `--apply` paths with `--force` bypass. Han's `dedupCrossTeam.ts` was wired in his own commit. 3 helper tests green. Manual verify: `--apply` refuses with friendly multi-line message + PID + start time when a fake server runs; `--apply --force` proceeds. Updated both copilot-instructions files (repo + `~/.github/`). |

## Apply step (orchestrator)

- 15:25 — Backed up DB to `data/lacrosse.db.bak-pre-h6-cross-team`.
- Verified no servers running (pgrep clean, port 3001 free).
- Ran `pnpm --filter @pll/ingest dedup:cross-team -- --apply`:
  - **87 duplicate name groups** processed, **90 player rows merged** (some groups had 3 instances: Smith ×3, Riley Smith ×3, Young ×3), **120 stat rows repointed**, **0 collisions**.
  - players: 1924 → **1834**; player_stats unchanged at 6162 (no rows lost).
- `PRAGMA integrity_check = ok`. 0 cross-team dups remain.
- Restarted dev server (cleared WAL/SHM); `/api/health` returns ok with new counts.

## Wave H6 retro

- ✅ All 3 lanes shipped clean. Lane 1 dedup discovery (87 dups vs the 4 hinted in the plan) showed the value of doing a fresh enumeration query inside the agent rather than trusting the plan's known-list.
- ✅ Lane 3 `pgrep` guard already paid off: Han's commit pulled in Leia's helper mid-task and wired it himself with no orchestration friction. The `--force` escape valve makes the check non-disruptive.
- ⚠️ Cross-lane file leak: Yoda's `git add -A` swept Leia's two new files into commit `1d0c01a` (content untouched). Lesson: agents working in parallel should `git add` only their owned paths, not `-A`. Add to skill.
- ⚠️ Surprise scale: 87 cross-team dups means the H5 composite-name split exposed a much larger latent dedup backlog than visible in the immediate aftermath. Worth a periodic sweep going forward.
- 🪪 Next likely candidates (Wave H7):
  - User-blocked queues: `2026-04-23-laxnumbers-aliases.csv` (102 rows) + `2026-04-23-team-score-reconcile-queue.json` (29 rows) once user provides decisions.
  - Wire `snapshot:db` into a scheduled GitHub Actions workflow once `AZURE_STORAGE_CONNECTION_STRING` secret is set.
  - Ingest hardening: surface composite-name + cross-team-dup warnings inside the regular ingest pipeline so they don't accumulate silently between dedicated migrations.
  - Add player-page sparkline + season totals card (visual polish; the data is already there).
